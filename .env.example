# Copy to .env and fill in. Never commit the filled-in file.
#   set -a && source .env && set +a

# OpenAI models (gpt-4o, o1, o3, o4-mini) and the default judge
OPENAI_API_KEY=

# DeepSeek (deepseek-chat, deepseek-reasoner)
DEEPSEEK_API_KEY=

# Aggregator fronting the Gemini / Claude / Qwen baselines
TAXR_PROXY_API_KEY=
TAXR_PROXY_BASE_URL=https://api2.aigcbest.top/v1
